###############################################################################
#
#  GFS with multiple tasks, no quilting, and frequent output
#  32 tasks / 1 thread
#
###############################################################################

export TEST_DESCR="GFS, 32 proc, 1 thread, no quilt, output every 4 timestep"

export RUNDIR=${RUNDIR_ROOT}/gfs_16_32
export CNTL_DIR=GFS_NODFI
export LIST_FILES="sigf03 sigf06 sigf12 sigf24 sigf48 \
                   sfcf03 sfcf06 sfcf12 sfcf24 sfcf48 \
                   flxf03 flxf06 flxf12 flxf24 flxf48"

export_gsm

export NSOUT=4
export QUILT=.false.
export PE1=32
export WTPG=1
export WLCLK=80
export WRTGP=1

RUN_SCRIPT=rt_gfs.sh
