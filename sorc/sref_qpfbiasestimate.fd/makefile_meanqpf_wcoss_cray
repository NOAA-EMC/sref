SHELL=/bin/sh
SRCS 	= estimate_meanqpfbias.f
OBJS	= estimate_meanqpfbias.o

FC 	= ftn
LDFLAGS =
LIBS 	= ${W3NCO_LIB4} ${W3EMC_LIB4} ${BACIO_LIB4}
CMD 	= sref_estimate_meanqpfbias

all:	$(CMD)

$(CMD):	 ${OBJS}
	$(FC) $(LDFLAGS) -o $(@) $(OBJS) $(LIBS)

clean:
	rm -f *.o $(CMD)
